[package]
name = "nexuszero-crypto"
version = "0.1.0"
edition = "2021"
authors = ["Stephen Bilodeau <sgbilod@gmail.com>"]
description = "Quantum-resistant zero-knowledge proof system based on lattice cryptography"
license = "UNLICENSED"
repository = "https://github.com/iamthegreatdestroyer/Nexuszero-Protocol"
keywords = ["cryptography", "zero-knowledge", "lattice", "post-quantum", "zkp"]
categories = ["cryptography", "mathematics"]

[dependencies]
# Matrix operations for lattice computations
ndarray = "0.15"

# Random number generation
rand = "0.8"

# Hashing for Fiat-Shamir transform
sha3 = "0.10"
sha2 = "0.10"

# Large integer arithmetic
num-bigint = "0.4"
num-traits = "0.2"
num-integer = "0.1"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"

# Memory security
zeroize = { version = "1.7", features = ["derive"] }

# Constant-time operations
subtle = "2.5"

# Error handling
thiserror = "1.0"

# Logging
log = "0.4"
anyhow = "1.0"

# Iterator utilities for batch verification
itertools = "0.12"

# Parallel iteration
rayon = "1.10"

[dev-dependencies]
# Benchmarking
criterion = { version = "0.5", features = ["html_reports"] }

# Property-based testing
proptest = "1.4"

# Test utilities
hex = "0.4"

[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

[profile.bench]
opt-level = 3
debug = false

[[bench]]
name = "crypto_benchmarks"
harness = false

[[bench]]
name = "comprehensive_benchmarks"
harness = false

[[bench]]
name = "proof_benchmarks"
harness = false

[[bench]]
name = "bulletproof_benchmarks"
harness = false

[[bench]]
name = "ntt_bench"
harness = false

[features]
default = []
simd = []
avx2 = ["simd"]
neon = ["simd"]

[lib]
name = "nexuszero_crypto"
path = "src/lib.rs"
crate-type = ["rlib", "cdylib"]
